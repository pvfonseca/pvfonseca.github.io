# IDEs
.idea/

# Hugo
resources/
public/
jsconfig.json
node_modules/
go.sum
.hugo_build.lock

# Pinned Hugo binary downloaded by scripts/hugo.ps1 / scripts/hugo.sh
.hugo-bin/

# LaTeX intermediate build files (xelatex's own binary intermediates, not
# needed once resume.pdf is compiled)
*.xdv
xelatex*.fls
